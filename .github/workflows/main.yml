# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # 22 is the active LTS and what Vercel deploys with (engines: 22.x),
        # so CI tests the same runtime production uses.
        node-version: [22.x]

    env:
      # CI runs against the brighteyes_test database (derived below), never live data.
      SECRET: ci-only-test-secret
      NEXT_PUBLIC_SITE_URL: http://localhost:3000

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          # cache: "npm"
      - run: npm ci
      - run: npm run lint
      - run: npm run check-format
      - run: npx jest

      - name: Point MONGO_URI at the test database
        env:
          BASE_MONGO_URI: ${{ secrets.MONGO_URI }}
        run: node -e "const uri = require('./scripts/testDbUri')(process.env.BASE_MONGO_URI); require('fs').appendFileSync(process.env.GITHUB_ENV, 'MONGO_URI=' + uri + '\n')"

      - name: Seed the test database
        run: node scripts/seedTestDb.js

      - run: npm run build

      - name: Cypress tests
        # v4 runs on the retired Node 16 actions runtime; v6 has the same
        # interface and runs on Node 20+.
        uses: cypress-io/github-action@v6
        with:
          install: false
          start: npm start
